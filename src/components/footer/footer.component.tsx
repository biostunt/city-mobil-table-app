import React from 'react';
import style from '../../styles/footer.module.css';

const Footer = () => (
    <div className={style.container}>
        <div className="block-description">footer</div>
    </div>
)

export default React.memo(Footer);